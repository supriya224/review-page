/** @type {import('next').NextConfig} */
const nextConfig = {
    // images:{
    //     domains:
    //       [  "ayuvya.com"]
        
    // }
    images: {
        domains: ["ayuvya.com"],
      },
};

export default nextConfig;
